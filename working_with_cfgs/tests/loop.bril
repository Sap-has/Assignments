@main {
  a: int = const 0;
.loop:
  a: int = add a 1;
  br a .loop .end;
.end:
  ret;
}

# ARGS: --analysis=pathlen
# OUT: Function main:
# OUT: pathlen: {'b0': 0, 'loop': 1, 'end': 2}

# ARGS: --analysis=rpo
# OUT: Function main:
# OUT: rpo: ['b0', 'loop', 'end']

# ARGS: --analysis=backedges
# OUT: Function main:
# OUT: backedges: [('loop', 'loop')]

# ARGS: --analysis=reducible
# OUT: Function main:
# OUT: reducible: True